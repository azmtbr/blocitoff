
      <tr>
        <td><%= link_to item.name, item %></td>
        <td><%= item.days_left %> days remaining </td>
        <td><button type="button" class="btn btn-danger"><%= link_to "", item, method: :delete, class: 'glyphicon glyphicon-ok' %></button></td>
      </tr>
  
